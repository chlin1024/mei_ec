export enum FinancialStatus {
  PENDING = 'pending',
  PAID = 'paid',
  REFUND = 'refund',
}

export enum FulfillmentStatus {
  PENDING = 'pending',
  FULFILLED = 'fulfilled',
  SALE_RETURN = 'sale_return',
}
